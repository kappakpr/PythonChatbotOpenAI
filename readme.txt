install the required libraries
	pip install Flask
	pip install chatterbot
	pip install chatterbot_corpus
	pip install openai
set the API key as an environment variable export OPENAI_API_KEY='yourkeyvaluehere'
run the Flask app python3 pythonfilename.py
